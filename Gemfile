source "https://rubygems.org"

# Locks dependencies to GitHub Pages production environment
gem "github-pages", group: :jekyll_plugins

# Fixes missing 'tainted?' method removed in newer Ruby versions
gem "tainted"

# Windows-specific timezone data support
gem "tzinfo-data", platforms: [:mingw, :mswin, :x64_mingw, :jruby]

# gem 'github-pages'
gem "csv"
gem 'bigdecimal'
# gem 'wdm', '>= 0.1.0'
gem "webrick" # Required for Ruby 3.0+ to run the local server
# gem "liquid", "~> 4.0"